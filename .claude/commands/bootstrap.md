# /bootstrap — KHÔNG DÙNG cho iFan

⛔ **iFan đã dựng xong.** Lệnh này để tạo dự án Node/TS **mới** từ con số 0 —
nó sẽ ghi đè `CLAUDE.md`, `.claude/settings.json`, `package.json`, `tsconfig.json`.

Cần thêm thứ gì vào iFan thì làm thẳng, đừng chạy khuôn dựng lại.
Muốn hiểu cấu trúc kho: đọc `AGENTS.md`, hoặc `docs/SO-DO-HE-THONG.md`.
